<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lewis.tangramdemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lesson1.L1Activity" />
        <activity android:name=".lesson1_click.L1Activity_click" />
        <activity android:name=".lesson1_scroll.L1Activity_scroll" />
        <activity android:name=".lesson2.L2Activity" />
        <activity android:name=".lesson3_exposure.L3Activity" />
        <activity android:name=".lesson4_load.L4Activity" />
        <activity android:name=".lesson5_support_module.L5Activity"/>
    </application>

</manifest>